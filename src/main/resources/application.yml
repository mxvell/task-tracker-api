server:
  port: 8080

spring:
  datasource:
     driver-class-name: org.postgresql.Driver
     url: jdbc:postgresql://localhost:5432/task-tracker-db
     username: maksym
     password: maksym

  jpa:
    hibernate:
      ddl-auto: update